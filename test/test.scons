Import('env')

import os


source = [
    'main.cpp'
]

CPPFLAGS='-Wall -Werror -DLOG_LEVEL_DEF=3 -g -Wno-unused-function'
CPPPATH = [ '.', os.path.join(env['gtest_dir'], 'include')]
    
LIBS = [ 'NopeLib', 'gtest', 'm', 'rt', 'pthread']
LIBPATH = [ '../main', '../gtest' ]
main = env.Program(target = 'main', source = source, LIBS = LIBS, 
    CPPPATH=CPPPATH, CPPFLAGS = CPPFLAGS, LIBPATH= LIBPATH)

