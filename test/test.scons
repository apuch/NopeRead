Import('env')

source = [
    'main.cpp',
]

CFLAGS='-Wall -Werror -DLOG_LEVEL_DEF=3 -g -Wno-unused-function -std=gnu99'
LIBS = [ 'NopeLib', 'm', 'rt', 'pthread']
LIBPATH = [ '../main' ]
env.Program(target = 'main', source = source, LIBS = LIBS, 
    CPATH='.', CFLAGS = CFLAGS, LIBPATH= LIBPATH)

